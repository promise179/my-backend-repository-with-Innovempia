# Q1: save as creates duplicate files which can be lost but it is the opposite for git(commit)
# Q2: git config --global user.name and git config --global user.email
# Q3:Repositories are folders tracked by git on a local computer
# Q4: git commit is to take a snapshot of the code but git push is to upload it 
# Q5: Origin stands for the cloud server(GitHub)
# Q6: git revert is  safe to use because it takes bad snapshots of your code and reverts them
# Q7: git rebase can rewrite the history of the code causing choas
# Q8: git checkout -b "new branch" 
# Q9: cloning downloads a copy of the repository while forking creates a server side copy of the repository under your account
# Q10: Branch protection rules
